import React from 'react';

const styles = {
    background: {
        backgroundColor: 'black'
    }
}

const Footer = () => {
    return (
        <footer style={styles.background}>This is the footer</footer>
    );
} 

export default Footer;